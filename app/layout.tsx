export const metadata = {
  title: 'Image Generator',
  description: 'Generate custom images with canvas',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
